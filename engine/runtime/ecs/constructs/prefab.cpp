#include "prefab.h"
// #include "utils.h"

EntityType prefab::instantiate()
{
  return {};
	// std::vector<EntityType> out_data;
	// if(!data)
	// 	return {};

	// if(!ecs::utils::deserialize_data(*data, out_data))
	// 	return {};

	// if(out_data.empty())
	// 	return {};
	// else
	// 	return out_data.front();
}
